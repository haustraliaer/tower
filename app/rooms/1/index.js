/**
 * @jsx React.DOM
 */

var React = require('react')

// for each room, create a door.

var Room = React.createClass({
  render: function() {
    return (
      <div>test room one</div>
    )
  },
})

module.exports = Room
