/**
 * @jsx React.DOM
 */

var React = require('react')

// for each room, create a door.

var Room = React.createClass({
  render: function() {
    return (
      <div>room 0</div>
    )
  },
})

module.exports = Room

